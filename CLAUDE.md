# Pay Jung — ข้อตกลงในการเขียนโค้ด

ร้านเติมเกม: หลังร้านสำหรับพนักงาน (`src/app/(app)`) + หน้าเว็บลูกค้า (`src/app/shop`)
Next.js App Router + Supabase Auth + Postgres · คอมเมนต์และข้อความบนหน้าจอเป็นภาษาไทย

## การต่อ API ผู้ให้บริการเติมเกม (บังคับทุกเจ้า)

ทุกเจ้าที่เอามาต่อ ต้องทำครบทุกข้อนี้ ไม่ใช่เฉพาะเจ้าที่เคยมีปัญหา
ตัวอย่างที่ทำครบแล้วดูได้ที่ [`src/lib/providers/jcr.ts`](src/lib/providers/jcr.ts)

### ① ช่องกรอกของลูกค้าต้องมาจากผู้ให้บริการเสมอ

**ห้ามเดาเอง ห้ามฮาร์ดโค้ดว่า "ขอ UID ช่องเดียวพอ"** — แต่ละเกมขอไม่เหมือนกัน
(Lineage2M ของ JCR ขอ Role ID + เลือก Server, บางเกมขอภูมิภาค, บางเกมขอไอดี+รหัสผ่าน)
ส่งไปไม่ครบ = ออเดอร์ถูกปฏิเสธ หรือแย่กว่านั้นคือ **เติมเข้าผิดเซิร์ฟเวอร์ ซึ่งเอาเงินคืนไม่ได้**

- ดึงรายการช่องกรอกมาเก็บใน `CatalogEntry.fields` ตอน `fetchCatalog`
  แล้วระบบจะสร้างช่องกรอกบนหน้าเว็บลูกค้าให้เองทั้งหมด (`components/BuyForm.tsx`)
- **คีย์ (`key`) ต้องตรงกับที่ปลายทางต้องการเป๊ะ** เพราะถูกส่งกลับไปตรง ๆ ตอนสั่งซื้อ
- ช่องที่มีตัวเลือกจำกัด (เซิร์ฟเวอร์ / ภูมิภาค) ต้องส่ง `options` มาด้วย
  ให้ลูกค้าเลือกจากรายการ ไม่ใช่พิมพ์เอง
- ถ้าคำตอบ JSON ไม่มีข้อมูลนี้ **ต้องหาทางอื่นจนได้** อย่าปล่อยผ่าน —
  เช่น JCR ให้มาเป็นแบบฟอร์ม HTML (`?format=html`) ก็ดึงมาแปลง (ดู `parseHtmlForm`)

### ② ยิง API ผ่านตัวคุมจังหวะกลางเสมอ

ใช้ [`src/lib/providers/http.ts`](src/lib/providers/http.ts) — ห้ามเรียก `fetch` ตรง ๆ โดยไม่ผ่าน `limited()`

- `pacerFor(key, gapMs)` — ตัวคุมจังหวะประจำบัญชี ต้องใช้**ตัวเดียวกัน**ทั้งตอนดึงรายการ
  ตอนสั่งออเดอร์ และตอนตามสถานะ ไม่งั้นงานพวกนี้จะไปแย่งโควตากันเอง
- นับ **HTTP 429 และ 5xx เป็นข้อผิดพลาดที่ลองใหม่ได้** และอ่าน `Retry-After` มาใช้ตรง ๆ
  แทนการเดาเวลารอเอง
- เส้นที่มีคนรออยู่หน้าจอ (ลูกค้ากดซื้อ) ให้ยอมแพ้เร็ว (`INTERACTIVE_MAX_WAIT_MS`)
  แล้วปล่อยให้เครื่องยนต์ส่งออเดอร์เอาเข้าคิวใหม่ ดีกว่าค้างหน้าเว็บ

### ③ งานยาวต้องมีเส้นตาย และห้ามเงียบเมื่อทำไม่ครบ

Vercel ตัดการทำงานที่ 60 วินาที ถ้าโดนตัดกลางคัน **ของที่ทำมาทั้งหมดหายและหน้าเว็บพัง**

- ตั้งงบเวลาไว้ต่ำกว่าเพดานเสมอ เผื่อเวลาบันทึกลงฐานข้อมูล
- ห้ามนอนรอเกินเส้นตาย แม้ปลายทางจะสั่งให้รอ — จบรอบแล้วเก็บของที่ได้มาก่อน
- ทำไม่ครบให้คืน `CatalogResult` พร้อม `partial: true` และ `note` บอกว่าขาดอะไรเพราะอะไร
  (`partial` จะทำให้ระบบไม่ล้างของเดิมทิ้ง และกดซ้ำเพื่อสะสมให้ครบได้)
- **"ได้ไม่ครบแต่บอกความจริง" ดีกว่า "ดูเหมือนครบ"** เสมอ

### ④ กฎเหล็กเรื่องเงินและการเติมซ้ำ

- สถานะที่ไม่รู้จัก = **ยังไม่จบ** ห้ามเดาว่าสำเร็จหรือล้มเหลว
- คืนเครดิตลูกค้าอัตโนมัติได้เฉพาะตอนปลายทางแจ้งชัดว่าล้มเหลว/ยกเลิกเท่านั้น
  ไม่แน่ใจให้พักไว้ให้คนตรวจ (`attention`)
- ส่งออเดอร์ซ้ำได้เฉพาะเมื่อปลายทางยืนยันว่า **ไม่เคยได้รับ** (`missing`)
  สรุปไม่ได้ให้ตอบ `unknown` ซึ่งระบบจะไม่ส่งซ้ำเด็ดขาด
- ส่งราคาทุนไปให้ปลายทางตรวจถ้าทำได้ และปฏิเสธเมื่อราคาปลายทางแพงกว่าทุนที่บันทึกไว้

### ⑤ ลงทะเบียนให้ครบ

`PROVIDER_KIND_META` ใน `providers/constants.ts` → `ADAPTERS` ใน `providers/registry.ts`
ส่วนที่เหลือ (ตัดเครดิต ล็อกกันส่งซ้ำ ตามสถานะ คืนเงิน) ใช้ของกลางร่วมกันหมด ไม่ต้องเขียนใหม่

## ทั่วไป

- ข้อความ error ที่คนหลังร้านเห็น ต้องบอกว่า**ต้องไปแก้ตรงไหน** ไม่ใช่แค่บอกว่าพัง
- ฐานข้อมูลต่อได้ทีละคำสั่ง (`max: 1`) — ยิงหลายคำสั่งพร้อมกันจะค้าง ให้รวมเป็นคำสั่งเดียวหรือยัดเป็นก้อน
- ตรวจก่อนส่งงานเสมอ: `npx tsc --noEmit` แล้ว `npm run build`

## ปรับโครงสร้างฐานข้อมูล

แก้ `src/lib/schema.ts` เมื่อไหร่ **ต้องเลื่อน `SCHEMA_VERSION` ใน `src/lib/db.ts` ทุกครั้ง**
ไม่งั้นระบบจะถือว่าโครงสร้างครบแล้วและข้ามการปรับ คอลัมน์ใหม่จะไม่ถูกสร้าง

และ **ห้ามให้คิวรีหลักของหน้าไปพึ่งคอลัมน์ที่เพิ่งเพิ่ม** — การเพิ่มคอลัมน์ต้องขอล็อกตาราง
ซึ่งอาจไม่สำเร็จเพราะมีคนเปิดหน้าเว็บค้างไว้ (แดชบอร์ดยิงถามทุก 10 วินาที)
ให้ถามแยกแล้วกลืน error ไว้ (`.catch(() => null)`) จะได้เสียแค่ฟีเจอร์ใหม่ ไม่ใช่ทั้งหน้าล่ม

## เรื่องที่ตัดสินใจไว้แล้ว — อย่าเปลี่ยนเองโดยไม่ถาม

- **24BUYM: แตกเป็นคนละแพ็กเกจต่อเซิร์ฟเวอร์ ไม่ใช้ดรอปดาวน์** (เจ้าของร้านเลือกไว้ 18 ส.ค. 2569)
  เจ้านี้ไม่ส่งรายการช่องกรอกมาเลย มีแค่ `servers[]` ต่อเกม · ของที่นำเข้าไปแล้วผูกกับเซิร์ฟเวอร์รายแพ็กเกจ
  การเปลี่ยนเป็นดรอปดาวน์จะทำให้ของเดิมกลายเป็นรายการซ้ำที่ต้องมาไล่ลบ
- **ราคาขึ้นหน้าเว็บลูกค้าเองอัตโนมัติหลังดึงราคาเสร็จ** (เจ้าของร้านเลือกไว้ 22 ส.ค. 2569)
  ทั้งรอบ cron และปุ่มในหลังร้านทำเหมือนกัน · ข้อยกเว้นเดียวคือ **แพ็กที่ราคาขายต่ำกว่าทุน
  จะไม่ถูกเผยแพร่** เพราะขายออกไปคือขาดทุนทุกบิล — ของพวกนี้ค้างราคาเดิมบนเว็บแล้วแจ้งเข้า LINE
  ปุ่ม "อัปเดตราคาขึ้นหน้าเว็บ" ยังต้องมีอยู่ สำหรับราคาที่คนแก้เองในหลังร้าน

- **รหัสสินค้าของ OverTopup ซ้ำกันข้ามชนิดได้ ห้ามรวมเป็นตัวเดียวกัน** (เจอจริง 21 ส.ค. 2569)
  เจ้านี้นับเลขสินค้าแยกกันคนละชุดต่อชนิด — รหัส `4/16` ของชนิด `uid` คือ "RoV Mobile 6,200 คูปอง"
  ส่วนของชนิด `card` คือ "Steam wallet TH 200 บาท" · `provider_catalog` จึงต้องมี `product_type`
  อยู่ในดัชนีที่ห้ามซ้ำ และคิวรีที่จับคู่แพ็กเกจกับรายการกลาง**ต้องเทียบ `product_type` ด้วยเสมอ**
  ถ้าลืม ตัวหลังจะทับตัวหน้าเงียบ ๆ แล้วราคาทุนของเกมจะกลายเป็นราคาบัตรเงินสด (4,700 → 205 บาท)
  ตัวชี้ขาดว่าแพ็กไหนคู่กับรายการไหนคือ **ชื่อเกม** เพราะไม่เคยถูกรอบดึงราคาเขียนทับ

- **ไม่ดึงสินค้าประเภท ID-Pass ของ OverTopup** (เจ้าของร้านเลือกไว้ 18 ส.ค. 2569)
  ประเภทนี้ต้องให้ลูกค้ากรอกไอดี+รหัสผ่านเกมผ่านหน้าเว็บเรา ซึ่งเป็นความเสี่ยงที่ไม่คุ้ม
  ถ้าข้อมูลรั่วคือบัญชีเกมลูกค้าหาย · `placeOrder` ของ OverTopup จึงปฏิเสธชนิดนี้ไว้ด้วย
