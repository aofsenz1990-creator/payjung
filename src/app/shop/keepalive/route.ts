import { NextResponse } from 'next/server'

/**
 * ช่องเคาะบอกเซิร์ฟเวอร์ว่า "ยังมีคนนั่งใช้งานอยู่"
 *
 * ตัวจับเวลาฝั่งเซิร์ฟเวอร์ต่ออายุให้ตอนเปิดหน้าใหม่เท่านั้น
 * ถ้าลูกค้านั่งกรอกไอดีเกมอยู่ในกล่องสั่งซื้อนาน ๆ โดยไม่เปลี่ยนหน้าเลย
 * ฝั่งเซิร์ฟเวอร์จะนึกว่าทิ้งเครื่องไปแล้ว พอกดสั่งซื้อจริงก็โดนเด้งออกและของที่กรอกหายหมด
 * หน้าเว็บจึงเคาะมาทางนี้เป็นระยะขณะที่ยังมีการขยับจริง
 *
 * ตัวต่ออายุอยู่ใน middleware — ที่นี่แค่ตอบกลับเปล่า ๆ ให้จบเร็วที่สุด
 * ใช้ POST เพื่อไม่ให้เบราว์เซอร์หรือ CDN เอาไปแคชหรือดึงล่วงหน้าเอง
 */
export async function POST() {
  return new NextResponse(null, { status: 204 })
}

export const dynamic = 'force-dynamic'
